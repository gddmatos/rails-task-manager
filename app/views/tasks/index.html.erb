<h1>Tasks</h1>

<ul>
    <% @tasks.each do |task| %>
        <li>
            <%= link_to task.name.capitalize, task_path(id: task.id) %> - 
            <%= task.description %>
        </li>
    <% end %>
</ul>

<%= link_to "Add a new task", new_task_path %>