<%= form_for(@task) do |task| %>
    <%= task.text_field :name, placeholder: "Enter the name of the task" %>
    <%= task.text_field :description, placeholder: "Enter the description of the task" %>
    <%= task.submit %>
<% end %>